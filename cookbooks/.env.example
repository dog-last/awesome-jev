# Get an API key at https://typesafe.ai (early access) or call through
# the Vercel AI Gateway (typesafe-ai/jev, no waitlist).
TYPESAFE_API_KEY=ts-your-key-here

# Optional: set to 1 if every request hangs and ends in TypeSafeAPITimeoutError
# (some networks drop the post-quantum TLS 1.3 ClientHello).
# JEV_TLS12=1
